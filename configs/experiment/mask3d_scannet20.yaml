defaults:
  - mask3d_scannet200
  - _self_

# override some parameters
general:
  checkpoint: "ckpts/mask3d-scannet20_val.ckpt"
  project_name: mask3d_scannet20
  experiment_name: mask3d_scannet20
  num_targets: 19
  output_dir: /datasets/scannet_masks/mask3d_scannet20

data:
  num_labels: 20
  remove_small_group: -1

model:
  _target_: src.models.networks.mask3d.mask3d.Mask3D
  num_queries: 150
