defaults:
  - segment3d_scannetpp
  - _self_

# override some parameters
general:
  project_name: segment3d_scannet
  experiment_name: segment3d_scannet
  dbscan_eps: 0.95
  dbscan_min_points: 1
  scannet_dir: /datasets/scannet_frames/scans
  output_dir: /datasets/scannet_masks/segment3d
