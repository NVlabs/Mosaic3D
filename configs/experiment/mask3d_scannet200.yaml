defaults:
  - segment3d_scannet
  - _self_

# override some parameters
general:
  checkpoint: "ckpts/mask3d.ckpt"
  project_name: mask3d_scannet200
  experiment_name: mask3d_scannet200
  num_targets: 201
  output_dir: /datasets/scannet_masks/mask3d_scannet200

data:
  num_labels: 200
  remove_small_group: -1

model:
  _target_: src.models.networks.mask3d.mask3d.Mask3D
  num_queries: 150
  num_classes: ${general.num_targets}
