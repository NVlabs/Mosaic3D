defaults:
  - ours_openvocab_instance

train_dataset:
  _target_: src.data.scannet.dataset.ScanNet200Dataset
  # class labels
  ignore_class_idx: [0, 2]

val_dataset:
  _target_: src.data.scannet.dataset.ScanNet200Dataset
